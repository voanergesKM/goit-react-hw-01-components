import styled from 'styled-components';
import {
  color,
  layout,
  flexbox,
  shadow,
  space,
  typography,
} from 'styled-system';

export const Box = styled('div')(
  color,
  layout,
  flexbox,
  shadow,
  space,
  typography
);
